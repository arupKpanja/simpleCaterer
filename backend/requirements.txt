# Caterer System — Part 1 (Planning & quoting) MVP
# Python 3.10+ (developed/tested on 3.12)

fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.5,<3.0
pydantic-settings>=2.1,<3.0
SQLAlchemy>=2.0,<2.1
psycopg[binary]>=3.1,<4.0  # Postgres driver (used by the Docker Compose stack)
langgraph>=0.2,<0.3
langgraph-checkpoint-sqlite>=2,<3
httpx>=0.26,<1.0
mcp>=1.0  # optional MCP server for the tool layer (Python >= 3.10)

# dev / test
pytest>=7.4,<9.0
